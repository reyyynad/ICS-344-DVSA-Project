Lesson 8 — Logic Vulnerability (Race Condition)  (documented-only)

Owner   : team member responsible for lessons 3–6 / 8 / 10.
Target  : DVSA-ORDER-COMPLETE (payment) vs DVSA-ORDER-UPDATE (itemList).
Primitive: no pessimistic / conditional-write lock on the order record.
           Submit card details, then within ~50 ms fire an "update"
           action that bumps itemList from 1 to 5 — order finalizes as
           paid with 5 items.
Fix     : conditional UpdateItem with
           ConditionExpression: "orderStatus < :processing";
           DynamoDB optimistic-lock version attribute; reject "update"
           on orders with orderStatus >= 200.

Suggested screenshots to place here:

  01-order-before-race.png         initial order state: status + itemList=1
  02-race-script.png               script firing the two requests ~50ms apart
  03-dynamodb-after-race.png       DynamoDB item showing paid (status 200+) + itemList=5
  04-timeline-cloudwatch.png       CloudWatch invocation timeline across both Lambdas
  05-after-fix-conditional.png     (optional) same race after fix → second update rejected
